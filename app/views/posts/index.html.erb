<h1>投稿一覧ページ</h1>
<%= link_to "新規登録", new_post_path %>
<div class="contents">
  <% @posts.each do |post| %>
    <div>
      <%= post.id %><%= post.contents %><%= link_to "編集", edit_post_path(post) %>
    </div>
  <% end %>
</div>